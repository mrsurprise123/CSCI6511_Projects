To run the program, you need to put the graph document and .py document in the same fold.
Then, input the file name which only has the filename, fromIndex, toIndex. You will get the
path from formIndex to toIndex and path cost and execution time of dijkstra and A*. I have
put verticers and edges in the same file.